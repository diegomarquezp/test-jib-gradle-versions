/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.9.4/userguide/building_java_projects.html
 */
buildscript {
    repositories {
        mavenCentral() // Or jcenter(), etc.
    }
    configurations.all {
        resolutionStrategy {
            eachDependency {
                if (requested.group == 'com.google.guava' && requested.name == 'guava') {
                    useVersion('32.1.2-jre')
                    because('Jib plugin requires a specific Guava JRE variant')
                }
            }
        }
    }
}

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
    id 'com.google.cloud.tools.jib' version '3.5.0'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // This dependency is used by the application.
    implementation 'com.google.guava:guava:29.0-jre'
}

application {
    // Define the main class for the application.
    mainClass = 'test.jib.gradle.versions.App'
}

